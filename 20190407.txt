1. Настроить nginx с php-fpm.
2. Настроить nginx в качестве балансировщика. Используя mod_upstram, раскидывать весь входящий трафик по трем Apache2-серверам, 
находящимся в локальной сети.
3. Настроить сетевой фильтр, чтобы был доступ только к сервисам http и ssh.
4. * Настроить правила iptables, чтобы из внешней сети можно было обратиться только к портам 80 и 443. Запросы, идущие на порт 8080, 
перенаправлять на порт 80.
5. * Настроить доступ по ssh только из указанной сети.
Примечание. Задания 4-6 дано для тех, кому упражнений 1-3 показалось недостаточно.

Срок сдачи до: 9 апр., 18:00 +05:00



1. apt install nginx
   apt install php-fpm
   /etc/nginx/sites-avaliable/default раскомментируем location ~ \.php$ исправляем 7.0 на 7.2

2.  /etc/nginx/nginx.conf
    upstream http {
    server 127.0.0.1:8080 weight=5;
    server 192.168.0.10;
    server 192.168.0.20;
    }

   etc/nginx/sites-avaliable/default
   location / {
   proxy_pass http://http;
   }

3. iptables -A INPUT -p tcp --dport 22 -j ACCEPT 
   iptables -A INPUT -p tcp --dport 80 -j ACCEPT 

4. iptables -A INPUT -p tcp --dport 80 -j ACCEPT
   iptables -A INPUT -p tcp --dport 443 -j ACCEPT
   iptables -t nat -A PREROUTING -p tcp --dport 8080 -j REDIRECT --to-port 80

5. iptables -A INPUT -p tcp --source 192.168.1.0/24 --dport 22 -j ACCEPT
